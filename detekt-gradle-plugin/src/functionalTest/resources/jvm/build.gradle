plugins {
    id("org.jetbrains.kotlin.jvm")
    id("io.gitlab.arturbosch.detekt")
}

repositories {
    mavenCentral()
    exclusiveContent {
        forRepository {
            ivy {
                url = uri("../../build/repo")
            }
        }
        filter {
            includeGroup("io.gitlab.arturbosch.detekt")
        }
    }
}

tasks.detektMain {
    exclude("**/Callee.kt")
}
