config:
  validation: true
  excludes: 'test>.*'

processors:
  active: true
  exclude:
    - 'DetektProgressListener'

console-reports:
  active: true
  exclude:
    - 'FileBasedFindingsReport'

potential-bugs:
  UnusedUnaryOperator:
    aliases: ['an-alias']
